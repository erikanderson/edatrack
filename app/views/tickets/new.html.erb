<%= error_messages_for(@ticket) %>
<%= simple_form_for :ticket, url: tickets_path do |f| %>
  <%= f.input :title %>
  <%= f.input :text, :as => :text, :input_html => { :cols => 50, :rows => 5 }%>
  <%= f.button :submit %>
<% end %>

